version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:80"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: demo
    ports:
      - "5433:5432"
